const mongoose = require("mongoose");
const connection = async(req,res)=>{").then(()=>{
            console.log("Database is connected");
        })
    }catch(error){
      (error);
    }
}
module.exports =connection;
